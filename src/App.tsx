import { ReactElement } from "react";

import { Header } from "./components";

import "./global.css";

export const App = (): ReactElement => {
  return (
    <div>
      <Header />
    </div>
  );
};
